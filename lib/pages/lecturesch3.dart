import 'package:flutter/material.dart';

class Lecturech3Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("Chapter 3-Lectures"),
        ),
        //body:
      ),
    );
  }
}
