import 'package:flutter/material.dart';

class Lecturech1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("Chapter 1-Lectures"),
        ),
        //body:
      ),
    );
  }
}
